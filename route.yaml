kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: pacmangit
  namespace: pacmangit
spec:
  host: >-
    pacman.dev-adv.net
  to:
    kind: Service
    name: pacmangit
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None